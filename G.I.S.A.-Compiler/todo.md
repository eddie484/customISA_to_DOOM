### 구현했지만 수정이 필요한 부분
++, --
switch문의 조건검사.
함수별 스택 할당 시, 해당 함수에 필요한 만큼의 크기만 할당하기. 현재는 모든 변수 크기만큼을 각각의 함수에 할당하고 있음.

큰 크기의 정수값 이용할 때, imm 크기를 벗어나는 경우에 대한 처리필요. (컴파일러와 어셈블러 모두 수정 필요.)

### 메모

brother가 있다면 컨테이너로 감싸라.

### 구현이 필요한 별도의 기능
간단한 링커
여러 파일 지원(일단은 그냥 파일 각각 컴파일 후 수동으로 해도 될듯)

조건문에 상수가 들어가는 경우, 0이면 false, 그 외면 true로 처리되도록 해야 함. ex: (i = x - 2) 가 조건판단에 들어갈 경우, x가 2면 false, 그 외에는 true로 판단될 수 있도록. 현재는 임시방편으로 sub에 항상 s플래그가 붙도록 emitter를 수정했지만, 보다 근본적인 수정이 필요할듯.


### 다음으로 할 순서
fpga cpu에 uart 합치기 -> 완!
uart와 cpu 활용해 printf scanf 역할의 함수 만들기
cpu에서 돌아가는 간단한 프로그램 만들기!
 - 계산기
 - 선택지 텍겜
 - 초간단 탑뷰 rpg: 맵 있고 주인공 이동하다가 아이템 얻기/적 만나면 jrpg 전투하기!
교재 10장 만들기
 fpga cpu에서 나눗셈 만들기
